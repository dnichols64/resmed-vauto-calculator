<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ResMed Auto BiPAP Calculator</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="container">
        <h1>ResMed Auto BiPAP Calculator</h1>

        <p class="note">
            IMPORTANT NOTE FOR SETTING YOUR RESMED AIRCURVE VAUTO:
            To ensure your machine accepts these calculated settings correctly and to avoid unexpected auto-adjustments during the input process, it is highly recommended to set the Max IPAP value first on your device. After setting Max IPAP, you can then proceed to adjust Min EPAP and Pressure Support (PS) to the calculated values shown below. This approach aligns with how the device processes pressure changes and helps prevent the machine from overriding your intended settings prematurely.
        </p>

        <div class="input-section">
            <h2>Doctor's Prescribed Settings:</h2>
            <div class="input-group">
                <label for="desiredMinEpap">Desired Min EPAP (cmH2O):</label>
                <input type="number" id="desiredMinEpap" step="0.2" min="4.0" max="25.0">
            </div>
            <div class="input-group">
                <label for="desiredMaxIpap">Desired Max IPAP (cmH2O):</label>
                <input type="number" id="desiredMaxIpap" step="0.2" min="4.0" max="25.0">
            </div>
            <div class="input-group">
                <label for="desiredPs">Desired PS (cmH2O):</label>
                <input type="number" id="desiredPs" step="0.2" min="0.0" max="20.0">
            </div>
            <button id="calculateButton">Calculate Machine Settings</button>
        </div>

        <div class="output-section">
            <h2>Actual Machine Settings:</h2>
            <div class="output-group">
                <label for="actualMinEpap">Actual Machine Min EPAP (cmH2O):</label>
                <input type="text" id="actualMinEpap" readonly>
            </div>
            <div class="output-group">
                <label for="actualMaxIpap">Actual Machine Max IPAP (cmH2O):</label>
                <input type="text" id="actualMaxIpap" readonly>
            </div>
            <div class="output-group">
                <label for="actualPs">Actual Machine PS (cmH2O):</label>
                <input type="text" id="actualPs" readonly>
            </div>
        </div>
    </div>

    <script src="script.js"></script>
</body>
</html>